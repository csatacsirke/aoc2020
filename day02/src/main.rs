use std::path::Path;
use std::fs;
use std::collections::HashSet;
use regex::Regex;


fn part1(input: &Vec<String>) -> String {
    panic!();
}

fn part2(input: &Vec<String>) -> String {
    panic!();
}

fn main() -> Result<(), aoc2020::Error> {
    aoc2020::run_with_test("day02", part1, None);

    return Ok(());
}